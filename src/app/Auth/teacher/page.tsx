'use client';
import React from 'react';

import SignInUserTeacher from '@/components/auth/SignInTeacher';

const page = () => {
  return (
    <div>
      <SignInUserTeacher />
    </div>
  );
};

export default page;
